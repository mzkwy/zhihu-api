# action.sendMessage

```javascript
action.sendMessage(uname, msg)
```

Send message to someone.

For example:

```javascript
api.action.sendMessage('zhihuadmin', 'hello')
    .then(console.log)
    .catch(console.trace)
// { r: 0, msg: '' }
```
