# api._request

### get(url[, params])

Send a `GET` HTTP request.

### post(url, data)

Send a `POST` HTTP request.

### request(opts)

Send a HTTP request.

### xsrf()

Get `_xsrf`.

### setCookie(cookie)

Set cookies.

### headers

HTTP request headers.
